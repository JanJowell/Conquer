{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "RAILPACK"
    },
    "deploy": {
        "startCommand": "php artisan schedule:run --verbose --no-interaction",
        "cronSchedule": "*/5 * * * *",
        "restartPolicyType": "NEVER"
    }
}
